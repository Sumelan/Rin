{
services = {
	  gvfs.enable = true;
	  tumbler.enable = true;	
	  udev.enable = true;
	  envfs.enable = true;
	  dbus.enable = true;
	  fwupd.enable = true;
	  upower.enable = true;	

# don’t shutdown when power button is short-pressed.
    logind.extraConfig = ''
      HandlePowerKey = ignore
    '';
  };
}
